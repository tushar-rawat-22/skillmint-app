NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only. Never prefix this value with NEXT_PUBLIC_ or expose it to clients.
SUPABASE_SECRET_KEY=
# Optional public contact shown on the privacy notice. Must be externally verified before release.
SKILLMINT_PRIVACY_CONTACT_EMAIL=
# Analytics collection defaults off. Absent or empty values are disabled; setting values does not authorize rollout.
NEXT_PUBLIC_ANALYTICS_COLLECTION_ENABLED=
ANALYTICS_COLLECTION_ENABLED=
# Server-only founder UUID. Absent or empty is disabled; setting a value does not authorize rollout.
ANALYTICS_FOUNDER_USER_ID=
